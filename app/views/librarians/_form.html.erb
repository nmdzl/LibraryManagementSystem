<%= form_with(model: librarian, local: true) do |form| %>
  <% if librarian.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(librarian.errors.count, "error") %> prohibited this librarian from being saved:</h2>

      <ul>
        <% librarian.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :librarian_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :librarian_email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.password_field :password, id: :librarian_password %>
  </div>

  <div class="field">
    <%= form.label :library %>
    <%= form.select(:library_id, Library.all.collect{ |l| [l.name, l.id] }) %>
  </div>


  <!--div class="field">
    <%= form.label :librarian %>
  <%= form.check_box :librarian, id: :librarian_librarian %>
  </div-->

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
