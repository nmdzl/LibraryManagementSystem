<%= form_with(model: special_collection_request, local: true) do |form| %>
  <% if special_collection_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(special_collection_request.errors.count, "error") %> prohibited this special_collection_request from being saved:</h2>

      <ul>
      <% special_collection_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.number_field :student_id, id: :special_collection_request_student_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.number_field :book_id, id: :special_collection_request_book_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
